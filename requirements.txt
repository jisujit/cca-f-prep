# CCA-F prep — direct dependencies (pinned to the versions used on the original machine)
# Recreate the environment with:  pip install -r requirements.txt
# See docs/SETUP.md for full machine-setup instructions.

# --- Anthropic API ---
anthropic==0.86.0
python-dotenv==1.2.2

# --- HTTP / data (also pulled in transitively by anthropic, pinned for reproducibility) ---
httpx==0.28.1
pydantic==2.12.5

# --- Jupyter / notebooks (jupyterlab provides the `jupyter lab` / notebook UI) ---
jupyterlab==4.5.6
ipykernel==7.2.0
